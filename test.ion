if (3 <= 5) {
    // exit the process
    #asm {
        "mov x0, #42"
        "mov x16, #1"
        "svc #0"
    }
} else {
    #asm {
        "mov x0, #5"
        "mov x16, #1"
        "svc #0"
    }
}

